diff --git a/src/openclose.c b/src/openclose.c
index 4913349..fe692be 100644
--- a/src/openclose.c
+++ b/src/openclose.c
@@ -116,8 +116,12 @@ static void clear_after_fork(void)
 
 static inline void init_page_size(void)
 {
-	PAGE_SIZE = sysconf(_SC_PAGESIZE);
-	PAGE_SHIFT = ffs(PAGE_SIZE) - 1;
+#if defined(__loongarch_lp64)
+    PAGE_SIZE = 65536;
+#else
+    PAGE_SIZE = sysconf(_SC_PAGESIZE);
+#endif
+    PAGE_SHIFT = ffs(PAGE_SIZE) - 1;
 }
 
 static HSAKMT_STATUS init_vars_from_env(void)
diff --git a/src/queues.c b/src/queues.c
index f62e862..e587521 100644
--- a/src/queues.c
+++ b/src/queues.c
@@ -325,13 +325,21 @@ void *allocate_exec_aligned_memory_gpu(uint32_t size, uint32_t align,
 	flags.ui32.HostAccess = !DeviceLocal;
 	flags.ui32.ExecuteAccess = 1;
 	flags.ui32.NonPaged = nonPaged;
+#if defined(__loongarch64)
+        flags.ui32.PageSize = HSA_PAGE_SIZE_64KB;
+#else
 	flags.ui32.PageSize = HSA_PAGE_SIZE_4KB;
+#endif
 	flags.ui32.CoarseGrain = DeviceLocal;
 	flags.ui32.Uncached = Uncached;
+#if defined(__loongarch64)
+    flags.ui32.NoNUMABind = 1;
+#endif
 
 	/* Get the closest cpu_id to GPU NodeId for system memory allocation
 	 * nonPaged=1 system memory allocation uses GTT path
 	 */
+#if !defined(__loongarch64)
 	if (!DeviceLocal && !nonPaged) {
 		cpu_id = get_direct_link_cpu(NodeId);
 		if (cpu_id == INVALID_NODEID) {
@@ -340,6 +348,7 @@ void *allocate_exec_aligned_memory_gpu(uint32_t size, uint32_t align,
 		}
 	}
 
+#endif
 	size = ALIGN_UP(size, align);
 
 	ret = hsaKmtAllocMemory(DeviceLocal ? NodeId : cpu_id, size, flags, &mem);
